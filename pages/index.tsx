import Layout from "@/components/Layout";
import Hero from "@/components/Home/Hero";

const IndexPage = () => {
  return (
    <Layout between={true}>
      <Hero />
    </Layout>
  );
};

export default IndexPage;
